spring:
  application:
    name: project2
  cloud:
    config:
      uri: http://localhost:1201/
      profile: dev
      label: spring-cloud-config-repo-V2

server:
  port: 2001

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/